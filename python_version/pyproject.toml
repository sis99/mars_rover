[project]
name = "python_version"
version = "0.1.0"
description = "Mars Rover navigator"
requires-python = ">=3.14"
dependencies = [
    "pytest>=9.1.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.0",
    "ruff>=0.15.17",
]

[tool.pytest.ini_options]
pythonpath = [".."]
